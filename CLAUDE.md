# CLAUDE.md

자료를 넣으면 LLM 에이전트가 읽고 위키를 만들어 관리하는 데스크톱 앱. Electron + TypeScript.

**지금은 0단계다.** 폴더와 배선만 있고 기능은 전부 비어 있다. 실행하면 `unimplemented: ...` 로 죽는 게 정상이다.

## 이 문서를 읽는 법

- **굵은 한 줄이 규칙이고, 그 아래 한 줄이 이유다.**
- **`npm run lint`(ESLint)** 와 `tsc` 가 잡아 주는 것은 여기 없다. 틀리면 빨간불이 뜨므로 외울 필요가 없다.
- **여기 있는 것은 전부 "아무도 안 잡는 것"이다.** 틀리면 그대로 머지된다.
- **"N단계"** 는 README 의 단계 표를 가리킨다. 0단계(레포 골격)가 끝난 상태다.
- 배경이 궁금하면 [README.md](README.md) 를 읽어라. 이 문서는 README 에 없는 것만 적는다.

---

## 같은 이름이 두 가지를 가리킨다

**헷갈리면 여기를 먼저 본다.** 이름이 같아서 엉뚱한 곳을 고치게 되는 자리들이다.

| 이름        | 하나                                                    | 다른 하나                                                                                                 |
| ----------- | ------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| **lint**    | `npm run lint` = **ESLint.** 우리 소스의 경계 규칙 검사 | `npm run wiki:lint` = **제품 기능.** 사용자 볼트의 깨진 링크·고아·중복을 보고한다 (`agent/tasks/lint.ts`) |
| **git**     | 이 저장소                                               | **사용자 볼트**의 git. `core/git/` 은 이쪽이다 (§3)                                                       |
| **written** | `Written` 클래스 — 툴이 쓴 경로만 모은다                | `IngestResult.written` — **툴이 쓴 것 ∪ `extraPaths`.** 둘은 같지 않다                                    |
| **sources** | 볼트의 `sources/` 폴더 — 원본 파일                      | `Fm.sources` — 프론트매터 필드. **세션 로그(`.piecepool/sessions/`)도 여기 들어간다**                     |
| **index**   | `src/core/index/` — 링크 색인                           | `src/main/index.ts` 같은 `index.ts` — 진입점 파일                                                         |

파일 이름은 바꾸지 않는다. 설계문서가 `lint` 로 규정했고 `agent/tasks/*` 진입점은 동결이다.

---

## 1. 손대도 되는 곳 / 안 되는 곳

| 폴더                         |                                                                     |
| ---------------------------- | ------------------------------------------------------------------- |
| `src/core/` · `src/cli/`     | 작업 구역. 단 **본인 담당 구간만** 손댄다                           |
| `src/shared/`                | **동결.** 합의 없이 고치지 않는다                                   |
| `src/main/` · `src/preload/` | **7단계까지 스텁 유지.** 지금 채우지 않는다                         |
| `src/renderer/`              | **8단계까지 스텁 유지.** 지금 채우지 않는다                         |
| `docs/adr/legacy/`           | **읽기 전용.** 구 레포 원문 보존용이다. 내용도 포맷도 바꾸지 않는다 |
| `docs/superpowers/specs/`    | 설계문서. **합의 없이 고치지 않는다**                               |
| `docs/adr/`                  | 새 ADR 추가는 가능. 기존 ADR 수정은 합의                            |

담당 구간은 두 갈래다. **A = 문서 → wiki**(`ingest/` · `agent/tasks/ingest.ts`), **B = wiki → query**(`agent/tasks/query.ts` · 수확 · 세션 로그).
`vault/` · `index/` · `git/` · `agent/` · `llm/` 은 공유 구역이다.

---

## 2. 멈추고 물어라

**아래 상황에서는 코드를 쓰지 말고 멈춘 뒤 사용자에게 묻는다.** 짐작해서 진행하지 않는다.

1. **`// FROZEN:` 파일의 시그니처를 바꿔야 할 것 같을 때** — 상대 개발자의 코드가 멈춘다
2. **새 npm 패키지를 설치해야 할 것 같을 때** — `electron` 은 7단계, `react` 는 8단계다. 그 외는 §9 를 먼저 읽어라
3. **요청에 없는 `unimplemented` 스텁을 채워야 할 것 같을 때** — 남의 구간이거나 다음 단계 일이다
4. **`Fm` 에 프론트매터 필드를 더해야 할 것 같을 때** — 4필드는 동결이다
5. **두 설계문서가 어긋나는데 0단계 설계 §15 의 이탈 목록에 없을 때** — 새 충돌이다
6. **4단계(`ingest` + `agent`)에 착수하는데 `docs/adr/0002-*.md` 가 없을 때** — §9 참조
7. **사용자 볼트의 기존 파일을 지우거나 옮겨야 할 때** — 에이전트는 `wiki/` 와 `inbox/` 에만 쓴다

---

## 3. 절대 하지 마라

### `// FROZEN:` 이 붙은 파일을 합의 없이 고치지 마라

두 사람 사이의 계약이다. 한쪽이 바꾸면 다른 쪽 작업이 멈춘다. 근거는 0단계 설계 §3 · §8.

파일 첫 줄로 등급을 판별한다.

| 첫 줄        | 뜻                                               |
| ------------ | ------------------------------------------------ |
| `// FROZEN:` | 동결. **합의가 필요하다.** 범위가 함께 적혀 있다 |
| `// OWNER:`  | 가배치. 소유자가 자유롭게 바꾼다                 |
| 둘 다 없음   | 내부 구현. 소유자 단독                           |

동결은 14곳이고 **일부는 파일 전체가 아니라 특정 함수만**이다. 마커에 적힌 범위를 읽어라.

### 볼트의 git 과 이 레포의 git 을 혼동하지 마라

`src/core/git/` 은 **사용자 볼트**의 git 을 다룬다. 이 저장소가 아니다. 볼트는 사용자가 고른 별개 폴더이고 별개 저장소다.
`core/git/` 을 이 레포에 커밋해서 검증하지 마라. 임시 폴더에 볼트 픽스처를 만든다.

### 설계문서 내용을 이 문서나 코드 주석으로 베끼지 마라

복사본은 원본이 바뀔 때 조용히 어긋난다. 가리키기만 한다.

### 공유 타입을 다른 파일에 다시 정의하지 마라

`Note` · `LinkRef` · `Vault` · `Fm` 같은 모양을 어디서든 다시 선언하지 않는다. §4 참조.

---

## 4. 반드시 이렇게 하라

### 공유 타입은 `src/shared/types.ts` 에서만 가져온다

이 파일이 유일한 창구다. 같은 모양을 다른 곳에 또 만들면 두 정의가 갈라지고 IPC 경계에서 터진다.

### `Fm.sources` 는 `addSource()` 로만 건드린다

```ts
fm.sources = [...];        // 하지 마라. 타입은 통과하고 상대의 출처가 사라진다
addSource(fm, "...")       // 이렇게
```

`setSources` 는 일부러 만들지 않았다. 덮어쓰는 경로를 아예 없앤 것이다.

### `Written` 은 작업마다 새로 만들어 인자로 넘긴다

전역 인스턴스를 두면 두 작업의 커밋 경로가 섞여서, 되돌리기가 남의 파일까지 삼킨다.

### 커밋 경로에 호출부가 쓴 파일도 넣는다

세션 로그와 `.gitignore` 는 툴이 아니라 호출부가 쓰므로 `Written` 에 자동으로 안 들어온다. 빠뜨리면 커밋 밖으로 밀린다.

### 제목 정규화는 `normalizeTitle()` 하나만 쓴다

맵을 만드는 쪽과 조회하는 쪽이 다른 정규화를 쓰면 **볼트 전체가 깨진 링크**가 되는데, 타입은 아무것도 안 잡아 준다.

---

## 5. 스텁을 만났을 때

```ts
throw new Error("unimplemented: core/vault/notes.readNote");
```

**버그가 아니다.** 0단계의 정상 상태이고 45곳이 그렇다.
실동작하는 것은 `core/assets.ts` · `core/prompts/load.ts` · `main/ipc.ts` 의 `wrap()` 세 곳뿐이다.

- **요청받지 않은 스텁을 채우지 마라.** 지나가다 구현하지 않는다
- 실행이 `unimplemented` 로 죽는 것이 현재의 통과 조건이다. 성공이 아니다
- 채울 때는 에러 메시지의 경로 표기(`core/<모듈>.<함수>`)를 유지한다

---

## 6. 검증

```bash
npm ci && npx prettier --check . && npm run lint && npm run typecheck && npm test
```

CI 가 이 순서 그대로 돈다. 커밋 전에 전부 돌린다.
`npm test` 는 테스트가 0개면 실패하는 게 맞다. `passWithNoTests` 를 켜지 마라.

---

## 7. 사람에게 넘길 것

에이전트가 할 수 없는 검증이다. **대신 하려 들지 말고 사용자에게 요청한다.**

- **한글 학술 PDF 의 추출 품질** — 실제 논문을 원문과 눈으로 대조해야 안다
- **만들어진 위키 확인** — 옵시디언으로 볼트 폴더를 열어 본다. 뷰어를 만들지 않는다
- **UI 변경의 비포·애프터 스크린샷** — 8단계 이후. 리뷰 요청 전에 사용자에게 첨부를 요청한다

---

## 8. 커밋 · PR

Conventional Commits. 타입은 영어, 설명은 한국어.

```
fix: 툴 6종을 타입에 담고 inbox 계약을 못 박는다
docs: 설계문서에 파서 타입 반영
```

- `main` 에 직접 push 하지 않는다. feature 브랜치 → PR → 머지
- **브랜치 보호는 켜져 있지 않다.** 기계가 막지 않으므로 사람이 지킨다
- **PR 설명은 올리는 사람이 직접 쓴다.** 에이전트에게 대신 쓰게 하지 않는다
- **본인이 설명하지 못하는 변경은 PR 에 넣지 않는다**
- 머지 후 feature 브랜치를 지운다

---

## 9. 새 라이브러리를 쓰기 전에 `docs/adr/` 를 먼저 읽어라

이미 정해진 것이 있고, 값을 치르고 얻은 결론이다. 다른 것을 설치하기 전에 근거를 확인한다.

| 무엇            | 정해진 것                                         | 어디                               |
| --------------- | ------------------------------------------------- | ---------------------------------- |
| PDF 텍스트 추출 | `pdfjs-dist` **legacy 빌드** 단독. CMap 자산 필수 | 상위 §7.1 · legacy ADR-0005 · 0010 |
| 볼트 git        | `isomorphic-git`. 네이티브 의존성 없음            | 상위 §4.3                          |
| 마크다운 에디터 | CodeMirror 6                                      | legacy ADR-0004                    |
| 그래프 렌더링   | Cytoscape.js                                      | legacy ADR-0006                    |
| LLM 공급자      | Gemini 단일 (**멀티 모델 라우팅 검토 중**)        | legacy ADR-0009                    |
| OCR             | **v1 범위 밖.** 텍스트 0자면 `parse_failed`       | legacy ADR-0003                    |

### 진행 중인 결정

ADR-0001 의 **"에이전트가 파일을 직접 편집"** 이 재검토 중이다. 대안은 코드가 조율하고 LLM 이 각 단계의 판단자가 되는 **에이전틱 파이프라인**이다. 4단계 착수 전에 ADR-0002 를 확인하라 (§2-6).

---

## 10. 어떤 질문일 때 무엇을 펴나

| 질문                                                   | 문서                                                                         |
| ------------------------------------------------------ | ---------------------------------------------------------------------------- |
| 이 레포가 뭔가, 어디부터 보나                          | [README.md](README.md)                                                       |
| 이 타입 · 이 배선이 왜 이 모양인가                     | [0단계 골격 설계](docs/superpowers/specs/2026-09-06-repo-skeleton-design.md) |
| 기능 판단이 필요하다 (볼트 · 링크 · 에이전트 · 안전망) | [재구축 설계](docs/superpowers/specs/2026-09-05-electron-rebuild-design.md)  |
| 왜 Electron 으로 다시 짓나                             | [ADR-0001](docs/adr/0001-electron-shell-llm-wiki.md)                         |
| 이 라이브러리를 써도 되나                              | [구 레포 ADR](docs/adr/legacy/) (상태: 대체됨)                               |

두 설계문서가 어긋나면 **0단계 설계가 이긴다.** 의도적 이탈 11건이 그 문서 §15 에 표로 있다.

---

## 이 문서를 갱신하는 때

**에이전트가 "지적하신 부분이 맞습니다, 제가 틀렸습니다" 라고 말했다면, 이 문서에 빠진 게 있다는 뜻이다.**
사과는 다음번 같은 실수를 막지 못한다. 여기 적힌 한 줄은 막는다. 그 자리를 찾아 한 줄 추가한다.

| 이런 일이 생기면                 | 여기를 고친다                                  |
| -------------------------------- | ---------------------------------------------- |
| 같은 지적을 두 번 받았다         | §2 멈추고 물어라                               |
| 이름이 헷갈려 엉뚱한 곳을 고쳤다 | "같은 이름이 두 가지를 가리킨다" 표            |
| 타입은 통과하는데 의미가 틀렸다  | §4 반드시 이렇게 하라                          |
| 단계가 넘어갔다                  | §1 의 "7단계까지 스텁 유지" 같은 줄이 만료된다 |
| 새 ADR 이 생겼다                 | §9 표                                          |

**이 문서 자체는 동결이 아니다.** 발견한 사람이 그 PR 에 함께 넣는다. 따로 합의를 받지 않는다.
