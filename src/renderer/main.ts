// OWNER: 8단계 — React 를 설치한 뒤 채운다.
// app/ features/ ds/ store/ 하위 구성은 8단계에서 확정하므로 지금 만들지 않는다.

/** UI 와 상태만. fs·API 키·직접 네트워크 호출은 여기 없다. */
export function mount(): void {
  throw new Error("unimplemented: renderer/main.mount");
}
