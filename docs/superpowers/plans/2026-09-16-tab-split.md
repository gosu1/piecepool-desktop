# 탭 분할 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 탭을 잡아 오른쪽으로 끌면 화면이 좌/우 두 칸으로 갈라지고 그 탭이 오른쪽 칸으로 간다. 칸이 둘이면 끌어서 칸 사이를 오가고, 한 칸이 비면 도로 하나가 된다.

**Architecture:** `workspace` 스토어의 `tabs` + `activeTab` 을 `panes: Pane[]` + `activePane` 으로 바꾼다. 판정 로직 둘(`hitDropZone` · `compact`)을 순수 함수로 먼저 짓고 테스트를 붙인 뒤, 스토어와 화면이 그것을 쓴다. 드래그는 포인터 이벤트로 짓고 전용 스토어를 따로 둔다.

**Tech Stack:** Electron 44 · React 19 · zustand 5 · Tailwind v4 · vite · vitest (node)

**설계문서:** `docs/superpowers/specs/2026-09-16-tab-split-design.md`

## Global Constraints

모든 태스크에 적용된다.

- **FROZEN 파일을 하나도 건드리지 않는다.** 이 계획이 여는 FROZEN 은 **없다.** `src/shared/*` 는 읽지도 고치지도 않는다. 고쳐야 할 것 같으면 **멈추고 묻는다** (CLAUDE.md §2)
- **`unimplemented` 스텁을 하나도 채우지 않는다.** `src/core/` · `src/main/keys.ts` 는 **그대로 둔다**
- **`src/renderer/features/graph/GraphView.tsx` 를 한 줄도 고치지 않는다.** 설계 §7 의 결정이다
- **새 npm 패키지를 설치하지 않는다.** `zustand` 5 와 React 19 가 이미 있다
- **`src/renderer/` 는 `core/`·`main/`·`cli/` 를 import 할 수 없다** (lint 가 잡는다)
- **`.ts`/`.tsx` 확장자를 import 에 반드시 붙인다.** `verbatimModuleSyntax: true` — 타입만 쓰는 import 는 `import type` 을 붙인다
- **DOM 테스트를 만들지 않는다.** `vitest.config.ts` 는 `environment: "node"` · `include: ["src/**/*.test.ts"]` 다. `.tsx` 는 테스트 대상이 아니다 — 화면은 사람이 본다 (CLAUDE.md §7)
- **zustand 셀렉터가 매번 새 객체·새 배열을 만들면 안 된다.** `s.panes[i]?.tabs ?? []` 같은 셀렉터는 호출마다 새 배열을 내므로 `Object.is` 비교가 항상 실패해 **무한 리렌더**가 된다. 칸은 `s.panes[i]`(객체 참조)로 통째로 받는다
- **테스트는 140개 / 14파일에서 시작한다.** 태스크별 기대값이 각 태스크에 적혀 있다. **숫자가 다르면 멈추고 보고한다**
- **매 커밋 전 `npx prettier --write .` 을 돌린다.** CI 가 `--check` 로 돈다
- **커밋 메시지**는 Conventional Commits, 타입은 영어 설명은 한국어. 끝에 한 줄을 **문자 그대로** 붙인다

  ```
  Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
  ```

- **브랜치는 `feat/electron-shell`.** `main` 에 직접 커밋하지 않는다
- **작업 트리에 `docs/superpowers/specs/2026-09-16-note-rendering-design.md` 가 untracked 로 있다.** 다른 작업이다. **`git add .` 으로 쓸어 담지 마라** — 커밋은 항상 경로를 찍어서 한다
- **앱을 띄워 확인하는 절차**(Task 5 에만 있다):
  1. `tasklist | grep -ci electron.exe` 가 0인지 확인한다. **0이 아니면 죽이지 말고 멈춰서 보고하라** — 사용자가 앱을 열어 두고 있을 수 있다. 이미 떠 있으면 vite HMR 이 이미 반영했을 수 있으니 그대로 쓴다
  2. 새로 띄울 때는 `npm start` 하나면 된다. dev 서버까지 함께 뜬다(`scripts/dev.mjs`)
  3. 출력에서 `Unable to load preload script` 를 grep 한다. **나오면 실패다**
  4. 내가 띄웠으면 끝나고 0으로 되돌린다. `netstat -ano | grep ':5173 '` 로 잔여를 확인한다

## 설계에서 벗어난 자리 하나

설계 §4 의 `Pane` 에 **`id: number` 를 더한다.** 설계에는 없다.

`Shell` 이 칸을 `panes.map()` 으로 그릴 때 `key` 가 인덱스면, 왼쪽 칸이 접힐 때 React 가
`key=0` 을 재사용하면서 오른쪽 칸의 내용을 왼쪽 자리로 재조정한다. 그 안의 `GraphView` 는
새 인스턴스가 되어 배치를 잃는다 — 설계 §7 은 "칸을 **옮길 때만** 잃는다" 고 했는데
**탭을 닫을 때도 잃게 된다.** `id` 를 키로 쓰면 React 가 살아남은 칸의 서브트리를 그대로 옮긴다.

## 파일 구조

| 파일                                        | 책임                                   | 태스크 |
| ------------------------------------------- | -------------------------------------- | ------ |
| `src/renderer/store/panes.ts`               | `Pane` · `compact` — 순수 (새 파일)    | 1      |
| `src/renderer/store/panes.test.ts`          | `compact` 6건 (새 파일)                | 1      |
| `src/renderer/features/split/hit.ts`        | `DropArea` · `hitDropZone` — 순수 (새) | 2      |
| `src/renderer/features/split/hit.test.ts`   | `hitDropZone` 6건 (새 파일)            | 2      |
| `src/renderer/store/workspace.ts`           | `panes` · `activePane` · 액션 개정     | 3, 4   |
| `src/renderer/store/workspace.test.ts`      | 탭 테스트를 칸 모델로 개정 + 칸 12건   | 3, 4   |
| `src/renderer/app/Pane.tsx`                 | 탭줄 + 본문 한 벌 (새 파일)            | 3      |
| `src/renderer/app/Shell.tsx`                | 칸을 수만큼 그린다 + `DragLayer`       | 3, 5   |
| `src/renderer/app/TabStrip.tsx`             | 칸 인자 + 포인터 핸들러                | 3, 5   |
| `src/renderer/app/NoteView.tsx`             | 칸 인자                                | 3      |
| `src/renderer/features/split/drag.ts`       | 드래그 전용 스토어 (새 파일)           | 5      |
| `src/renderer/features/split/DragLayer.tsx` | 고스트 + 드롭 존 강조 (새 파일)        | 5      |

**태스크를 다섯으로 자른 기준**

|       | 검증 수단                                                                 |
| ----- | ------------------------------------------------------------------------- |
| 1 · 2 | 순수 함수. 아무것도 import 하지 않으므로 기존 동작을 못 깬다              |
| 3     | 모델·부품 교체. **동작은 그대로다** — 칸이 하나뿐이라 화면이 안 바뀐다    |
| 4     | 스토어에 이동/분할 능력. 화면에서 부를 길이 아직 없어 **테스트로만** 잰다 |
| 5     | 드래그 배선. **사람이 앱을 띄워야만** 확인된다                            |

---

### Task 1: `compact` — 빈 칸 정리

**Files:**

- Create: `src/renderer/store/panes.ts`
- Create: `src/renderer/store/panes.test.ts`

**Interfaces:**

- Consumes: `Tab` — `src/renderer/store/workspace.ts` 에 이미 있다 (**타입만** 가져온다)
- Produces:
  - `export interface Pane { id: number; tabs: Tab[]; activeTab: string | null }`
  - `export function compact(panes: Pane[], focus: number): { panes: Pane[]; activePane: number }`

**순환 import 에 대해:** `panes.ts` 가 `workspace.ts` 에서 `Tab` 을 **타입으로만** 가져오고,
`workspace.ts` 가 `panes.ts` 에서 `compact` 를 값으로 가져온다. `verbatimModuleSyntax` 아래
`import type` 은 컴파일에서 완전히 지워지므로 **런타임 순환이 생기지 않는다.**
`eslint.config.js` 에 `import/no-cycle` 규칙도 없다.

---

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/renderer/store/panes.test.ts` 를 새로 만든다.

```ts
import { describe, expect, it } from "vitest";
import { compact } from "./panes.ts";
import type { Pane } from "./panes.ts";
import type { Tab } from "./workspace.ts";

const tab: Tab = {
  kind: "note",
  id: "note:a.md",
  path: "a.md",
  title: "a",
  body: "",
  error: null,
  seq: 1,
};

/** 탭이 든 칸. id 는 호출부가 준다 — 같은 id 가 둘이면 안 된다. */
const full = (id: number): Pane => ({ id, tabs: [tab], activeTab: tab.id });
const empty = (id: number): Pane => ({ id, tabs: [], activeTab: null });

describe("compact", () => {
  it("빈 칸이 없으면 그대로 돌려준다", () => {
    const panes = [full(0), full(1)];
    const r = compact(panes, 1);
    expect(r.panes).toEqual(panes);
    expect(r.activePane).toBe(1);
  });

  it("앞 칸이 비면 걷어내고 focus 를 한 칸 당긴다", () => {
    const right = full(1);
    const r = compact([empty(0), right], 1);
    expect(r.panes).toEqual([right]);
    expect(r.activePane).toBe(0);
  });

  it("뒷 칸이 비면 걷어내고 focus 는 그대로다", () => {
    const left = full(0);
    const r = compact([left, empty(1)], 0);
    expect(r.panes).toEqual([left]);
    expect(r.activePane).toBe(0);
  });

  it("focus 칸 자체가 비면 0 이 된다", () => {
    const left = full(0);
    const r = compact([left, empty(1)], 1);
    expect(r.panes).toEqual([left]);
    expect(r.activePane).toBe(0);
  });

  it("전부 비면 첫 칸을 그대로 남긴다", () => {
    const first = empty(0);
    const r = compact([first, empty(1)], 1);
    expect(r.panes).toEqual([first]);
    expect(r.activePane).toBe(0);
  });

  it("focus 가 범위 밖이면 0 이 된다", () => {
    const only = full(0);
    const r = compact([only], 5);
    expect(r.activePane).toBe(0);
  });
});
```

- [ ] **Step 2: 돌려서 실패를 확인한다**

```bash
npx vitest run src/renderer/store/panes.test.ts
```

Expected: **FAIL.** `Failed to load url ./panes.ts` 또는 `Cannot find module` — 파일이 아직 없다.

- [ ] **Step 3: `compact` 를 구현한다**

`src/renderer/store/panes.ts` 를 새로 만든다.

```ts
import type { Tab } from "./workspace.ts";

/**
 * 칸 하나. 탭 줄과 본문이 한 벌이다.
 *
 * `id` 는 React 의 key 로 쓴다. 인덱스를 키로 주면 왼쪽 칸이 접힐 때
 * React 가 오른쪽 칸의 내용을 왼쪽 자리로 재조정해 GraphView 가 새로 마운트된다 —
 * 배치를 잃는 자리가 설계 §7 이 허용한 것보다 넓어진다.
 */
export interface Pane {
  id: number;
  tabs: Tab[];
  activeTab: string | null;
}

/**
 * 빈 칸을 걷어내고, 주목할 칸의 새 인덱스를 함께 돌려준다.
 *
 * 칸을 지우면 인덱스가 밀린다. moveTabToPane 과 closeTab 이 이 계산을 각자 하면
 * 한쪽만 틀리므로 한 자리에 모았다(설계 §4.1).
 *
 * `panes` 는 절대 빈 배열이 되지 않는다 — 전부 비면 첫 칸을 그대로 남긴다.
 * panes[0] 을 읽는 자리가 여럿이고, 빈 배열을 허용하면 그 전부에 가드가 붙는다.
 */
export function compact(panes: Pane[], focus: number): { panes: Pane[]; activePane: number } {
  const kept = panes.filter((p) => p.tabs.length > 0);
  if (kept.length === 0) return { panes: panes.slice(0, 1), activePane: 0 };
  // 참조로 찾는다 — filter 가 같은 객체를 넘겨주므로 indexOf 가 맞는다.
  const target = panes[focus];
  const next = target === undefined ? -1 : kept.indexOf(target);
  return { panes: kept, activePane: next === -1 ? 0 : next };
}
```

- [ ] **Step 4: 돌려서 통과를 확인한다**

```bash
npx vitest run src/renderer/store/panes.test.ts
```

Expected: **PASS.** `Tests 6 passed (6)`

- [ ] **Step 5: 정적 검사와 커밋**

```bash
npx prettier --write . && npm run lint && npm run typecheck && npm test
```

Expected: `lint`·`typecheck` 출력 없음. `test` 는 **`Test Files 15 passed (15)` · `Tests 146 passed (146)`**

```bash
git add src/renderer/store/panes.ts src/renderer/store/panes.test.ts
git commit -F - <<'EOF'
feat: 빈 칸을 걷어내는 compact 를 짓는다

칸을 지우면 인덱스가 밀린다. 이동과 닫기가 그 계산을 각자 하면
한쪽만 틀리므로 순수 함수 하나로 모은다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 2: `hitDropZone` — 드롭 존 판정

**Files:**

- Create: `src/renderer/features/split/hit.ts`
- Create: `src/renderer/features/split/hit.test.ts`

**Interfaces:**

- Consumes: 없다. 아무것도 import 하지 않는다
- Produces:
  - `export interface DropArea { left: number; width: number }`
  - `export function hitDropZone(x: number, area: DropArea, paneCount: number): number | null`

---

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/renderer/features/split/hit.test.ts` 를 새로 만든다.

```ts
import { describe, expect, it } from "vitest";
import { hitDropZone } from "./hit.ts";
import type { DropArea } from "./hit.ts";

/** 사이드바가 닫힌 화면. 왼쪽 끝이 0 이다. */
const flush: DropArea = { left: 0, width: 1000 };
/** 사이드바가 열린 화면. 본문이 오른쪽으로 밀려 있다. */
const shifted: DropArea = { left: 288, width: 800 };

describe("hitDropZone — 칸이 하나일 때", () => {
  it("왼쪽 끝은 제자리다", () => {
    expect(hitDropZone(0, flush, 1)).toBeNull();
  });

  it("오른쪽 25% 경계 바로 왼쪽은 제자리다", () => {
    expect(hitDropZone(749, flush, 1)).toBeNull();
  });

  it("오른쪽 25% 안이면 새 칸이다", () => {
    expect(hitDropZone(750, flush, 1)).toBe(1);
    expect(hitDropZone(999, flush, 1)).toBe(1);
  });

  it("area.left 가 0 이 아닐 때도 맞다", () => {
    // 288 + 800*0.75 = 888
    expect(hitDropZone(887, shifted, 1)).toBeNull();
    expect(hitDropZone(888, shifted, 1)).toBe(1);
  });
});

describe("hitDropZone — 칸이 둘일 때", () => {
  it("가운데 왼쪽은 왼쪽 칸이다", () => {
    expect(hitDropZone(0, flush, 2)).toBe(0);
    expect(hitDropZone(499, flush, 2)).toBe(0);
  });

  it("정확히 가운데부터는 오른쪽 칸이다", () => {
    expect(hitDropZone(500, flush, 2)).toBe(1);
    expect(hitDropZone(999, flush, 2)).toBe(1);
  });
});
```

- [ ] **Step 2: 돌려서 실패를 확인한다**

```bash
npx vitest run src/renderer/features/split/hit.test.ts
```

Expected: **FAIL.** `Failed to load url ./hit.ts` — 파일이 아직 없다.

- [ ] **Step 3: `hitDropZone` 을 구현한다**

`src/renderer/features/split/hit.ts` 를 새로 만든다.

```ts
/** 드롭 판정에 쓰는 본문 영역. 드래그 시작 때 한 번 재서 들고 있는다(설계 §5). */
export interface DropArea {
  left: number;
  width: number;
}

/** 칸이 하나일 때, 이 비율보다 오른쪽에 놓아야 갈라진다. */
const SPLIT_EDGE = 0.75;

/**
 * 포인터 x 가 어느 칸으로의 드롭인지. null 이면 제자리다(아무 일도 일어나지 않는다).
 *
 * 화면 좌표를 쓰는 판정을 컴포넌트 밖으로 뺀 이유는 하나다 — .tsx 는 테스트할 수 없고,
 * 드래그에서 가장 틀리기 쉬운 부분이 이 판정이다. features/graph/hit.ts 와 같은 자리다.
 */
export function hitDropZone(x: number, area: DropArea, paneCount: number): number | null {
  const r = (x - area.left) / area.width;
  if (paneCount === 1) return r >= SPLIT_EDGE ? 1 : null;
  return r < 0.5 ? 0 : 1;
}
```

- [ ] **Step 4: 돌려서 통과를 확인한다**

```bash
npx vitest run src/renderer/features/split/hit.test.ts
```

Expected: **PASS.** `Tests 6 passed (6)`

- [ ] **Step 5: 정적 검사와 커밋**

```bash
npx prettier --write . && npm run lint && npm run typecheck && npm test
```

Expected: **`Test Files 16 passed (16)` · `Tests 152 passed (152)`**

```bash
git add src/renderer/features/split/hit.ts src/renderer/features/split/hit.test.ts
git commit -F - <<'EOF'
feat: 드롭 존 판정을 순수 함수로 짓는다

칸이 하나면 오른쪽 25% 안에서만 갈라지고, 둘이면 가운데를 기준으로
어느 칸인지 고른다. .tsx 는 테스트할 수 없으므로 판정만 따로 뺀다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 3: 스토어와 화면을 칸 단위로 바꾼다

**이 태스크는 동작을 바꾸지 않는다.** 칸이 하나뿐이라 화면은 지금과 똑같아야 한다.
바뀌는 것은 모델과 부품 경계다.

**Files:**

- Modify: `src/renderer/store/workspace.ts`
- Modify: `src/renderer/store/workspace.test.ts`
- Create: `src/renderer/app/Pane.tsx`
- Modify: `src/renderer/app/Shell.tsx`
- Modify: `src/renderer/app/TabStrip.tsx`
- Modify: `src/renderer/app/NoteView.tsx`

**Interfaces:**

- Consumes: `Pane` · `compact` — `src/renderer/store/panes.ts` (Task 1)
- Produces:
  - `panes: Pane[]` · `activePane: number` — `WorkspaceState` 의 필드. **`tabs` 와 `activeTab` 은 사라진다**
  - `export function Pane({ index }: { index: number })` — `src/renderer/app/Pane.tsx`
  - `TabStrip` · `NoteView` 가 `{ pane: number }` 를 받는다

---

- [ ] **Step 1: 테스트를 칸 모델로 고쳐 쓴다**

`src/renderer/store/workspace.test.ts` 에서 **네 곳**을 고친다.

**(1)** import 에 `Pane` 타입을 더한다. 파일 맨 위 `import type { Result }` 줄 **아래**에 붙인다.

```ts
import type { Pane } from "./panes.ts";
```

**(2)** `beforeEach` 의 초기화를 칸 모델로 바꾼다.

```ts
beforeEach(() => {
  useWorkspace.setState({
    expanded: new Set(initial.expanded),
    selected: null,
    sidebarOpen: true,
    sidebarWidth: 240,
    panes: [{ id: 0, tabs: [], activeTab: null }],
    activePane: 0,
  });
});
```

**(3)** 테스트에서 칸을 읽고 세우는 자리를 위해, `beforeEach` **바로 아래**에 헬퍼 둘을 둔다.

```ts
/** 칸 0 의 탭들. 기존 테스트가 s.tabs 로 읽던 자리를 대신한다. */
function tabs0() {
  return useWorkspace.getState().panes[0].tabs;
}

/** 칸 0 의 활성 탭. */
function active0() {
  return useWorkspace.getState().panes[0].activeTab;
}
```

**(4)** `describe("applied")` 의 세 번째 테스트에서 탭을 보는 두 줄을 바꾼다.
기존에 `expect(next.tabs).toEqual([])` · `expect(next.activeTab).toBeNull()` 이던 자리를
아래로 바꾼다.

```ts
expect(next.panes).toEqual([{ id: 0, tabs: [], activeTab: null }]);
expect(next.activePane).toBe(0);
```

**(5)** `describe("탭")` · `describe("그래프 탭")` · `describe("쿼리 탭")` 세 블록에서
`useWorkspace.getState().tabs` → `tabs0()`, `.activeTab` → `active0()` 로 바꾸고,
`useWorkspace.setState({ tabs: [...], activeTab: ... })` 는 칸으로 감싼다.

예를 들어 `describe("그래프 탭")` 의 세 번째 테스트는 이렇게 된다.

```ts
it("노트 탭과 키 공간이 겹치지 않는다", () => {
  useWorkspace.setState({
    panes: [
      {
        id: 0,
        tabs: [
          {
            kind: "note",
            id: noteTabId("graph"),
            path: "graph",
            title: "graph",
            body: "",
            error: null,
            seq: 1,
          },
        ],
        activeTab: noteTabId("graph"),
      },
    ],
    activePane: 0,
  });
  useWorkspace.getState().openGraphTab();
  expect(tabs0()).toHaveLength(2);
});
```

그리고 `describe("탭")` 의 `applied` 관련 마지막 테스트, `describe("그래프 탭")`·`describe("쿼리 탭")`
의 "볼트를 바꾸면 … 함께 닫힌다" 셋은 아래 모양이 된다.

```ts
const next = applied({
  ok: true,
  value: { root: "/새볼트", name: "새볼트", tree: [] },
});
expect(next.panes).toEqual([{ id: 0, tabs: [], activeTab: null }]);
expect(next.activePane).toBe(0);
```

**테스트 개수는 늘지 않는다.** 이 단계는 같은 성질을 새 모델로 다시 재는 것이다.

- [ ] **Step 2: 돌려서 실패를 확인한다**

```bash
npx vitest run src/renderer/store/workspace.test.ts
```

Expected: **FAIL.** `panes` 를 `setState` 에 넘겼는데 스토어에 그런 필드가 없고
`panes[0]` 이 `undefined` 라 `Cannot read properties of undefined (reading 'tabs')` 가 난다.

- [ ] **Step 3: 스토어를 칸 모델로 바꾼다**

`src/renderer/store/workspace.ts` 를 고친다. **여섯 곳**이다.

**(1)** import 를 더한다. 파일 맨 위 `import type { TreeNode, VaultPayload }` 줄 **아래**에 붙인다.

```ts
import { compact } from "./panes.ts";
import type { Pane } from "./panes.ts";

// Shell·Pane 이 스토어에서 Pane 을 가져다 쓴다. 그 import 를 살려 둔다.
export type { Pane };
```

**(2)** 기존의 `let tabSeq = 0;` 줄과 그 위 주석을 찾아, **그 두 줄을 아래 블록 전체로 교체한다.**

```ts
/** 탭 요청 세대. 닫았다가 곧바로 다시 연 탭에 옛 응답이 덮어쓰는 것을 막는다. */
let tabSeq = 0;

/** 칸 id 세대. React 의 key 로 쓰므로 살아 있는 칸끼리 겹치지 않기만 하면 된다. */
let paneSeq = 0;

/** 탭이 든 칸의 인덱스. 없으면 -1. */
function paneOf(panes: Pane[], id: string): number {
  return panes.findIndex((p) => p.tabs.some((t) => t.id === id));
}

/** 칸에서 탭 하나를 뺀다. 뺀 것이 활성이었으면 오른쪽 이웃, 없으면 왼쪽으로 옮긴다. */
function removeTab(p: Pane, id: string): Pane {
  const idx = p.tabs.findIndex((t) => t.id === id);
  if (idx === -1) return p;
  const tabs = p.tabs.filter((t) => t.id !== id);
  const activeTab = p.activeTab === id ? (tabs[idx]?.id ?? tabs[idx - 1]?.id ?? null) : p.activeTab;
  return { ...p, tabs, activeTab };
}

/**
 * 빈 칸 하나. 볼트를 바꿀 때와 초기 상태가 쓴다.
 *
 * id 가 늘 0 인 것이 맞다 — 키는 **같은 시점에 살아 있는 칸끼리만** 겹치지 않으면 되고,
 * 이 칸이 놓이는 순간 다른 칸은 전부 사라진다. paneSeq 를 쓰면 테스트가 앞선 분할 횟수에
 * 따라 다른 값을 기대하게 된다.
 */
function firstPane(): Pane {
  return { id: 0, tabs: [], activeTab: null };
}

/**
 * 그래프·쿼리처럼 id 가 상수인 탭을 연다.
 * 어느 칸에든 이미 있으면 그 칸으로 포커스하고, 없으면 activePane 에 붙인다.
 */
function openFixed(s: WorkspaceState, tab: GraphTab | QueryTab): Partial<WorkspaceState> {
  const found = paneOf(s.panes, tab.id);
  if (found !== -1) {
    return {
      panes: s.panes.map((p, i) => (i === found ? { ...p, activeTab: tab.id } : p)),
      activePane: found,
    };
  }
  return {
    panes: s.panes.map((p, i) =>
      i === s.activePane ? { ...p, tabs: [...p.tabs, tab], activeTab: tab.id } : p,
    ),
  };
}
```

**(3)** `WorkspaceState` 인터페이스에서 `tabs` 와 `activeTab` 두 줄을 지우고 아래로 바꾼다.

```ts
  /** 열린 칸들. 길이는 1 또는 2 다. 절대 빈 배열이 되지 않는다. */
  panes: Pane[];
  /** 새 탭이 열릴 칸. */
  activePane: number;
```

**(4)** `applied()` 안의 `tabs: []` · `activeTab: null` 두 줄을 아래로 바꾼다.

```ts
    // 칸도 하나로 되돌린다. NotePath 는 볼트 루트 기준 상대경로라 새 볼트에서 다른 파일을 뜻한다 —
    // 남겨 두면 이전 볼트의 글이 새 볼트의 탭 제목을 달고 조용히 그대로 떠 있는다.
    panes: [firstPane()],
    activePane: 0,
```

**(5)** `create<WorkspaceState>` 의 초기값에서 `tabs: []` · `activeTab: null` 두 줄을 바꾼다.

```ts
  panes: [firstPane()],
  activePane: 0,
```

**(6)** 액션 다섯을 바꾼다. `openTab` · `openGraphTab` · `openQueryTab` · `focusTab` · `closeTab`
**전체**를 아래로 교체한다.

```ts
  openTab: async (path, title) => {
    const id = noteTabId(path);

    // 어느 칸에든 이미 열려 있으면 다시 읽지 않는다. 파일 감시가 없어 다시 읽어도
    // 최신이라는 보장이 없고, 보던 글이 갑자기 바뀌는 쪽이 더 나쁘다.
    const found = paneOf(get().panes, id);
    if (found !== -1) {
      set((s) => ({
        panes: s.panes.map((p, i) => (i === found ? { ...p, activeTab: id } : p)),
        activePane: found,
        selected: path,
      }));
      return;
    }

    const seq = ++tabSeq;
    // 먼저 타입을 붙여 둔다. map 안에서 리터럴로 쓰면 kind 가 string 으로 넓어진다.
    const tab: Tab = { kind: "note", id, path, title, body: null, error: null, seq };
    set((s) => ({
      panes: s.panes.map((p, i) =>
        i === s.activePane ? { ...p, tabs: [...p.tabs, tab], activeTab: id } : p,
      ),
      selected: path,
    }));

    let body: string | null = null;
    let error: string | null = null;
    try {
      const r = await window.piecepool.readRaw(path);
      if (r.ok) body = stripFrontmatter(r.value);
      else error = r.error.message;
    } catch (e) {
      // preload 가 없으면 여기로 온다. 탭은 열린 채 이유를 보여 준다.
      error = `앱 내부 연결이 끊겼다: ${String(e)}`;
    }

    // 모든 칸을 훑는다 — 읽는 동안 탭이 옆 칸으로 옮겨졌을 수 있다.
    // 세대까지 같아야 담는다. 그 사이에 닫혔거나 다시 열렸으면 이 응답은 낡은 것이다.
    set((s) => ({
      panes: s.panes.map((p) => ({
        ...p,
        tabs: p.tabs.map((t) =>
          t.kind === "note" && t.id === id && t.seq === seq ? { ...t, body, error } : t,
        ),
      })),
    }));
  },

  openGraphTab: () =>
    set((s) => openFixed(s, { kind: "graph", id: GRAPH_TAB_ID, title: "그래프" })),

  openQueryTab: () => set((s) => openFixed(s, { kind: "query", id: QUERY_TAB_ID, title: "쿼리" })),

  focusTab: (id) =>
    set((s) => {
      const i = paneOf(s.panes, id);
      if (i === -1) return {};
      const t = s.panes[i].tabs.find((x) => x.id === id);
      const panes = s.panes.map((p, j) => (j === i ? { ...p, activeTab: id } : p));
      // 그래프·쿼리 탭에는 경로가 없다 — 트리 선택을 건드리지 않는다.
      return t?.kind === "note"
        ? { panes, activePane: i, selected: t.path }
        : { panes, activePane: i };
    }),

  closeTab: (id) =>
    set((s) => {
      const i = paneOf(s.panes, id);
      if (i === -1) return {};
      const panes = s.panes.map((p, j) => (j === i ? removeTab(p, id) : p));
      return compact(panes, s.activePane);
    }),
```

- [ ] **Step 4: 스토어 테스트가 통과하는지 본다**

```bash
npx vitest run src/renderer/store/workspace.test.ts
```

Expected: **PASS.** 개수는 개정 전과 같다.

`npm run typecheck` 는 **아직 실패한다** — `TabStrip` 과 `NoteView` 가 없어진 `s.tabs` 를
읽고 있다. 다음 단계에서 고친다.

- [ ] **Step 5: `Pane.tsx` 를 만든다**

`src/renderer/app/Pane.tsx` 를 새로 만든다.

```tsx
import { NoteView } from "./NoteView.tsx";
import { TabStrip } from "./TabStrip.tsx";

/**
 * 칸 하나 — 탭 줄과 본문 한 벌.
 *
 * Shell 이 이것을 칸 수만큼 그린다. 지금까지 Shell 이 TabStrip 과 NoteView 를
 * 직접 나란히 놓았는데, 칸이 둘이 되면 그 한 벌을 두 번 그려야 한다.
 */
export function Pane({ index }: { index: number }) {
  return (
    <div className="flex min-w-0 flex-1 flex-col">
      <TabStrip pane={index} />
      <NoteView pane={index} />
    </div>
  );
}
```

- [ ] **Step 6: `TabStrip` 이 칸을 받게 한다**

`src/renderer/app/TabStrip.tsx` 에서 **함수 머리와 셀렉터만** 바꾼다. JSX 본문은 그대로다.

```tsx
/**
 * 한 칸의 탭 줄. 오른쪽 빈 공간은 일부러 비워 둔다 —
 * 거기가 창을 끄는 자리다(app-no-drag 를 붙이지 않는다).
 */
export function TabStrip({ pane }: { pane: number }) {
  // 칸을 통째로 받는다. s.panes[pane]?.tabs ?? [] 처럼 쓰면 호출마다 새 배열이 나와
  // Object.is 비교가 늘 실패하고 무한 리렌더가 된다.
  const p = useWorkspace((s) => s.panes[pane]);
  // 창 조작 버튼은 오른쪽 끝에만 있다 — 마지막 칸만 자리를 비운다.
  const last = useWorkspace((s) => pane === s.panes.length - 1);
  const focusTab = useWorkspace((s) => s.focusTab);
  const closeTab = useWorkspace((s) => s.closeTab);

  if (p === undefined || p.tabs.length === 0) return null;

  return (
    <div
      // macOS 는 우리 버튼을 안 그린다(OS 신호등이 왼쪽 위에 있다) — 비울 것이 없다.
      style={{ paddingRight: IS_MAC || !last ? 0 : WINDOW_CONTROLS_WIDTH }}
      // 탭이 넘치면 가로로 스크롤한다. 스크롤바가 나오면 32px 안에서 탭이 잘리므로 숨긴다.
      className="flex h-8 shrink-0 items-center gap-1 overflow-x-auto border-b border-hairline bg-chrome px-1 [scrollbar-width:none]"
    >
      {p.tabs.map((t) => {
        const active = t.id === p.activeTab;
```

나머지(탭 하나를 그리는 `<div>` 와 두 `<button>`)는 **한 글자도 바꾸지 않는다.**

- [ ] **Step 7: `NoteView` 가 칸을 받게 한다**

`src/renderer/app/NoteView.tsx` 의 `NoteView` 함수 **전체**를 아래로 바꾼다.
`noteBody` 는 그대로 둔다.

```tsx
/** 한 칸의 활성 탭 본문. 마크다운을 렌더하지 않는다 — 원문 그대로다(설계 §2). */
export function NoteView({ pane }: { pane: number }) {
  // 칸을 통째로 받는다 — 파생값을 셀렉터에서 만들면 매번 새 참조가 나온다.
  const p = useWorkspace((s) => s.panes[pane]);
  if (p === undefined) return null;

  const tab = p.tabs.find((t) => t.id === p.activeTab) ?? null;
  const graphOpen = p.tabs.some((t) => t.kind === "graph");

  return (
    <>
      {/* 그래프 탭이 이 칸에 있는 한 계속 마운트해 둔다 — 언마운트하면 재스캔·재배치로
          pan/zoom 과 노드 위치를 잃는다. 탭이 안 바뀌었을 땐 숨기기만 한다. */}
      {graphOpen && <GraphView hidden={tab === null || tab.kind !== "graph"} />}
      {noteBody(tab)}
    </>
  );
}
```

- [ ] **Step 8: `Shell` 이 칸을 수만큼 그린다**

`src/renderer/app/Shell.tsx` 에서 **세 곳**을 고친다.

**(1)** import 를 바꾼다. `NoteView` 와 `TabStrip` 은 이제 `Pane` 이 부른다.

```tsx
import { Fragment, useEffect, useState } from "react";
import type { KeyboardEvent as ReactKeyboardEvent, PointerEvent as ReactPointerEvent } from "react";
import { Pane } from "./Pane.tsx";
import { Ribbon } from "./Ribbon.tsx";
import { Sidebar } from "./Sidebar.tsx";
import { WindowControls } from "./WindowControls.tsx";
```

`NoteView` 와 `TabStrip` import 두 줄은 **지운다.**

**(2)** `Shell()` 안에서 칸 목록을 읽는다. `const sidebarOpen = …` 줄 **아래**에 붙인다.

```tsx
const panes = useWorkspace((s) => s.panes);
```

**(3)** `<main>` 블록을 아래로 바꾼다.

```tsx
<main className="flex min-w-0 flex-1">
  {panes.map((p, i) => (
    // key 는 인덱스가 아니라 칸 id 다. 인덱스면 왼쪽 칸이 접힐 때 React 가
    // 오른쪽 칸의 내용을 왼쪽 자리로 재조정해 GraphView 가 새로 마운트된다.
    <Fragment key={p.id}>
      {i > 0 && <div className="w-px shrink-0 bg-hairline" />}
      <Pane index={i} />
    </Fragment>
  ))}
</main>
```

- [ ] **Step 9: 정적 검사 넷을 돌린다**

```bash
npx prettier --write . && npm run lint && npm run typecheck && npm test
```

Expected: `lint`·`typecheck` 출력 없음. `test` 는 **`Test Files 16 passed (16)` · `Tests 152 passed (152)`** — Task 2 와 같다. 이 태스크는 테스트를 더하지 않는다.

- [ ] **Step 10: 커밋한다**

```bash
git add src/renderer/store/workspace.ts src/renderer/store/workspace.test.ts \
        src/renderer/app/Pane.tsx src/renderer/app/Shell.tsx \
        src/renderer/app/TabStrip.tsx src/renderer/app/NoteView.tsx
git commit -F - <<'EOF'
refactor: 탭을 칸 단위로 담는다

tabs + activeTab 을 panes + activePane 으로 바꾸고, 탭줄과 본문 한 벌을
Pane 으로 묶는다. 칸이 아직 하나뿐이라 화면 동작은 그대로다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 4: `moveTabToPane` — 분할과 이동

**Files:**

- Modify: `src/renderer/store/workspace.ts`
- Modify: `src/renderer/store/workspace.test.ts`

**Interfaces:**

- Consumes: `paneOf` · `removeTab` (Task 3, `workspace.ts` 내부) · `compact` (Task 1)
- Produces: `moveTabToPane: (id: string, to: number) => void` — `WorkspaceState` 의 액션

---

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/renderer/store/workspace.test.ts` **맨 끝**, `describe("쿼리 탭", …)` **다음에** 붙인다.

```ts
/** 노트 탭 하나를 만든다. 칸을 세우는 테스트가 반복해서 쓴다. */
function note(name: string): Tab {
  return {
    kind: "note",
    id: noteTabId(name),
    path: name,
    title: name,
    body: "",
    error: null,
    seq: 1,
  };
}

describe("moveTabToPane", () => {
  it("칸이 하나일 때 옮기면 칸이 둘이 된다", () => {
    const a = note("a.md");
    const b = note("b.md");
    useWorkspace.setState({
      panes: [{ id: 0, tabs: [a, b], activeTab: a.id }],
      activePane: 0,
    });
    useWorkspace.getState().moveTabToPane(b.id, 1);
    const s = useWorkspace.getState();
    expect(s.panes).toHaveLength(2);
    expect(s.panes[0].tabs.map((t) => t.id)).toEqual([a.id]);
    expect(s.panes[1].tabs.map((t) => t.id)).toEqual([b.id]);
    expect(s.panes[1].activeTab).toBe(b.id);
    expect(s.activePane).toBe(1);
  });

  it("두 칸의 id 가 서로 다르다", () => {
    const a = note("a.md");
    const b = note("b.md");
    useWorkspace.setState({
      panes: [{ id: 0, tabs: [a, b], activeTab: a.id }],
      activePane: 0,
    });
    useWorkspace.getState().moveTabToPane(b.id, 1);
    const s = useWorkspace.getState();
    expect(s.panes[0].id).not.toBe(s.panes[1].id);
  });

  it("칸이 둘일 때 반대 칸으로 옮기면 원래 칸에서 사라진다", () => {
    const a = note("a.md");
    const b = note("b.md");
    const c = note("c.md");
    useWorkspace.setState({
      panes: [
        { id: 0, tabs: [a], activeTab: a.id },
        { id: 1, tabs: [b, c], activeTab: c.id },
      ],
      activePane: 1,
    });
    useWorkspace.getState().moveTabToPane(c.id, 0);
    const s = useWorkspace.getState();
    expect(s.panes[0].tabs.map((t) => t.id)).toEqual([a.id, c.id]);
    expect(s.panes[1].tabs.map((t) => t.id)).toEqual([b.id]);
    expect(s.activePane).toBe(0);
  });

  it("떠난 칸의 활성 탭이 오른쪽 이웃으로 옮겨간다", () => {
    const a = note("a.md");
    const b = note("b.md");
    const c = note("c.md");
    useWorkspace.setState({
      panes: [{ id: 0, tabs: [a, b, c], activeTab: b.id }],
      activePane: 0,
    });
    useWorkspace.getState().moveTabToPane(b.id, 1);
    expect(useWorkspace.getState().panes[0].activeTab).toBe(c.id);
  });

  it("마지막 탭을 옮기면 빈 칸이 정리돼 칸이 도로 하나다", () => {
    const a = note("a.md");
    useWorkspace.setState({
      panes: [{ id: 0, tabs: [a], activeTab: a.id }],
      activePane: 0,
    });
    useWorkspace.getState().moveTabToPane(a.id, 1);
    const s = useWorkspace.getState();
    expect(s.panes).toHaveLength(1);
    expect(s.panes[0].tabs.map((t) => t.id)).toEqual([a.id]);
    expect(s.activePane).toBe(0);
  });

  it("같은 칸으로의 드롭은 아무것도 바꾸지 않는다", () => {
    const a = note("a.md");
    const before = [{ id: 0, tabs: [a], activeTab: a.id }];
    useWorkspace.setState({ panes: before, activePane: 0 });
    useWorkspace.getState().moveTabToPane(a.id, 0);
    expect(useWorkspace.getState().panes).toEqual(before);
  });

  it("없는 탭이나 범위 밖 칸은 아무것도 바꾸지 않는다", () => {
    const a = note("a.md");
    const before = [{ id: 0, tabs: [a], activeTab: a.id }];
    useWorkspace.setState({ panes: before, activePane: 0 });
    useWorkspace.getState().moveTabToPane("note:없다.md", 1);
    expect(useWorkspace.getState().panes).toEqual(before);
    // 칸은 둘까지다 — 2 는 만들지 않는다.
    useWorkspace.getState().moveTabToPane(a.id, 2);
    expect(useWorkspace.getState().panes).toEqual(before);
  });
});

describe("칸이 둘일 때의 기존 액션", () => {
  it("오른쪽 칸의 마지막 탭을 닫으면 칸이 하나가 되고 activePane 이 0 이다", () => {
    const a = note("a.md");
    const b = note("b.md");
    useWorkspace.setState({
      panes: [
        { id: 0, tabs: [a], activeTab: a.id },
        { id: 1, tabs: [b], activeTab: b.id },
      ],
      activePane: 1,
    });
    useWorkspace.getState().closeTab(b.id);
    const s = useWorkspace.getState();
    expect(s.panes).toHaveLength(1);
    expect(s.panes[0].tabs.map((t) => t.id)).toEqual([a.id]);
    expect(s.activePane).toBe(0);
  });

  it("focusTab 은 탭이 든 칸으로 activePane 을 옮긴다", () => {
    const a = note("a.md");
    const b = note("b.md");
    useWorkspace.setState({
      panes: [
        { id: 0, tabs: [a], activeTab: a.id },
        { id: 1, tabs: [b], activeTab: b.id },
      ],
      activePane: 0,
    });
    useWorkspace.getState().focusTab(b.id);
    expect(useWorkspace.getState().activePane).toBe(1);
  });

  it("그래프 탭을 열면 activePane 쪽에 붙는다", () => {
    const a = note("a.md");
    const b = note("b.md");
    useWorkspace.setState({
      panes: [
        { id: 0, tabs: [a], activeTab: a.id },
        { id: 1, tabs: [b], activeTab: b.id },
      ],
      activePane: 1,
    });
    useWorkspace.getState().openGraphTab();
    const s = useWorkspace.getState();
    expect(s.panes[0].tabs).toHaveLength(1);
    expect(s.panes[1].tabs).toHaveLength(2);
  });

  it("이미 열린 노트를 사이드바에서 다시 열면 그 탭이 있는 칸으로 간다", async () => {
    const a = note("a.md");
    const b = note("b.md");
    useWorkspace.setState({
      panes: [
        { id: 0, tabs: [a], activeTab: a.id },
        { id: 1, tabs: [b], activeTab: b.id },
      ],
      activePane: 1,
    });
    // 이미 열려 있으면 openTab 이 곧바로 돌아온다 — window.piecepool 을 부르지 않는다.
    await useWorkspace.getState().openTab("a.md", "a");
    const s = useWorkspace.getState();
    expect(s.activePane).toBe(0);
    expect(s.panes[0].activeTab).toBe(a.id);
    expect(s.panes[1].tabs).toHaveLength(1);
    expect(s.selected).toBe("a.md");
  });

  it("이미 열린 그래프 탭은 그 탭이 있는 칸으로 포커스가 간다", () => {
    const a = note("a.md");
    useWorkspace.setState({
      panes: [
        { id: 0, tabs: [a], activeTab: a.id },
        { id: 1, tabs: [{ kind: "graph", id: GRAPH_TAB_ID, title: "그래프" }], activeTab: null },
      ],
      activePane: 0,
    });
    useWorkspace.getState().openGraphTab();
    const s = useWorkspace.getState();
    expect(s.panes).toHaveLength(2);
    expect(s.activePane).toBe(1);
    expect(s.panes[1].activeTab).toBe(GRAPH_TAB_ID);
  });
});
```

`Tab` 타입을 테스트에서 쓰므로 import 에 더한다. 파일 맨 위 `import type { Pane }` 줄 **아래**에 붙인다.

```ts
import type { Tab } from "./workspace.ts";
```

- [ ] **Step 2: 돌려서 실패를 확인한다**

```bash
npx vitest run src/renderer/store/workspace.test.ts
```

Expected: **FAIL.** `TypeError: useWorkspace.getState(...).moveTabToPane is not a function`.

`describe("칸이 둘일 때의 기존 액션")` 4건은 **이미 통과한다** — Task 3 이 그 동작을 지었기
때문이다. 이 4건은 칸이 둘인 상태를 처음으로 재는 회귀 그물이다.

- [ ] **Step 3: `moveTabToPane` 을 구현한다**

`src/renderer/store/workspace.ts` 에서 **두 곳**을 고친다.

**(1)** `WorkspaceState` 인터페이스의 `closeTab: (id: string) => void;` **아래**에 선언을 더한다.

```ts
  closeTab: (id: string) => void;
  moveTabToPane: (id: string, to: number) => void;
```

**(2)** `closeTab` 구현 **아래**에 구현을 둔다.

```ts
  moveTabToPane: (id, to) =>
    set((s) => {
      // 칸은 둘까지다. to 가 panes.length 면 새 칸을 만든다는 뜻이다.
      if (to < 0 || to > 1 || to > s.panes.length) return {};
      const from = paneOf(s.panes, id);
      if (from === -1 || from === to) return {};
      const tab = s.panes[from].tabs.find((t) => t.id === id);
      if (tab === undefined) return {};

      // 새 칸을 만드는 경우와 기존 칸에 넣는 경우를 한 배열로 만들어 놓고 한 번에 훑는다.
      const grown: Pane[] =
        to === s.panes.length ? [...s.panes, { id: ++paneSeq, tabs: [], activeTab: null }] : s.panes;

      const panes = grown.map((p, i) => {
        if (i === from) return removeTab(p, id);
        if (i === to) return { ...p, tabs: [...p.tabs, tab], activeTab: id };
        return p;
      });

      // 떠난 칸이 비었으면 걷어낸다. 인덱스가 밀리므로 compact 가 새 activePane 을 준다.
      return compact(panes, to);
    }),
```

- [ ] **Step 4: 돌려서 통과를 확인한다**

```bash
npx vitest run src/renderer/store/workspace.test.ts
```

Expected: **PASS.**

- [ ] **Step 5: 정적 검사와 커밋**

```bash
npx prettier --write . && npm run lint && npm run typecheck && npm test
```

Expected: **`Test Files 16 passed (16)` · `Tests 164 passed (164)`** — 152 + 12.

```bash
git add src/renderer/store/workspace.ts src/renderer/store/workspace.test.ts
git commit -F - <<'EOF'
feat: 탭을 다른 칸으로 옮긴다

to 가 panes.length 면 칸을 새로 만든다 — 분할과 이동이 한 함수다.
떠난 칸이 비면 compact 가 걷어내고 activePane 을 다시 준다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 5: 드래그를 배선한다

**Files:**

- Create: `src/renderer/features/split/drag.ts`
- Create: `src/renderer/features/split/DragLayer.tsx`
- Modify: `src/renderer/app/TabStrip.tsx`
- Modify: `src/renderer/app/Shell.tsx`

**Interfaces:**

- Consumes: `hitDropZone` · `DropArea` (Task 2) · `moveTabToPane` · `focusTab` (Tasks 3·4)
- Produces:
  - `export const useDrag` — `src/renderer/features/split/drag.ts`
  - `export function DragLayer()` — `src/renderer/features/split/DragLayer.tsx`

**자동 테스트가 없는 태스크다.** `vitest` 는 `.test.ts` 만 잡고 `environment: "node"` 라
`.tsx` 도 포인터 이벤트도 볼 수 없다. 판정 로직은 Task 2 에서 이미 쟀다.
통과 조건은 **정적 검사 넷 + 사람이 앱을 띄워 보는 것**이다.

---

- [ ] **Step 1: 드래그 전용 스토어를 만든다**

`src/renderer/features/split/drag.ts` 를 새로 만든다.

```ts
import { create } from "zustand";
import { hitDropZone } from "./hit.ts";
import type { DropArea } from "./hit.ts";

/** 끌고 있는 탭의 신원. 고스트에 제목을 그린다. */
interface DragTab {
  id: string;
  title: string;
}

interface DragState {
  /** 끌고 있는 탭. null 이면 드래그 중이 아니다. */
  tab: DragTab | null;
  x: number;
  y: number;
  /** 드래그 시작 때 한 번 잰 본문 영역. */
  area: DropArea | null;
  /** 지금 떨구면 갈 칸. null 이면 제자리다. */
  zone: number | null;
  start: (tab: DragTab, area: DropArea) => void;
  move: (x: number, y: number, paneCount: number) => void;
  end: () => void;
}

/**
 * 드래그 중인 탭. workspace 스토어와 일부러 떼어 놓았다(설계 §6).
 *
 * 포인터가 움직일 때마다 workspace.set 이 돌면 Sidebar·Pane 둘·Ribbon 이 전부 다시
 * 그려진다. 이 스토어는 구독자가 DragLayer 하나뿐이라 고스트 div 만 다시 그려진다.
 * 덤으로 applied()(볼트 전환)나 closeTab 이 드래그를 몰라도 된다.
 */
export const useDrag = create<DragState>((set, get) => ({
  tab: null,
  x: 0,
  y: 0,
  area: null,
  zone: null,

  start: (tab, area) => set({ tab, area, zone: null }),

  move: (x, y, paneCount) => {
    const { area } = get();
    if (area === null) return;
    set({ x, y, zone: hitDropZone(x, area, paneCount) });
  },

  end: () => set({ tab: null, area: null, zone: null }),
}));
```

- [ ] **Step 2: `DragLayer` 를 만든다**

`src/renderer/features/split/DragLayer.tsx` 를 새로 만든다.

```tsx
import { useDrag } from "./drag.ts";

/**
 * 드래그 중에만 보이는 오버레이. 그리는 것은 둘이다 —
 * 포인터를 따라다니는 고스트와, 지금 놓으면 갈 칸의 강조.
 *
 * pointer-events-none 이라 아래 요소의 포인터 이벤트를 가로채지 않는다.
 * 이게 없으면 오버레이가 pointermove 를 삼켜 드래그가 그 자리에서 멈춘다.
 */
export function DragLayer() {
  const tab = useDrag((s) => s.tab);
  const area = useDrag((s) => s.area);
  const zone = useDrag((s) => s.zone);
  const x = useDrag((s) => s.x);
  const y = useDrag((s) => s.y);

  if (tab === null || area === null) return null;

  return (
    <div className="pointer-events-none fixed inset-0 z-50">
      {zone !== null && (
        <div
          // 칸이 하나일 때 zone 이 1 이면 오른쪽 절반이 새 칸이 될 자리다.
          // 판정 경계(75%)와 강조 영역(50%)이 다른 것은 의도한 것이다 —
          // 강조는 "놓으면 여기 생긴다" 를 보여 준다.
          style={{
            left: area.left + (zone === 0 ? 0 : area.width / 2),
            width: area.width / 2,
          }}
          className="absolute inset-y-0 border-2 border-primary bg-primary/10"
        />
      )}
      <div
        // 포인터 바로 아래 두면 커서가 글자를 가린다. 오른쪽 아래로 조금 민다.
        style={{ left: x + 8, top: y + 8 }}
        className="absolute h-6 w-40 truncate rounded-md bg-chrome px-2 text-sm leading-6 text-ink opacity-80 shadow"
      >
        {tab.title}
      </div>
    </div>
  );
}
```

- [ ] **Step 3: `TabStrip` 에 포인터 핸들러를 붙인다**

`src/renderer/app/TabStrip.tsx` 에서 **네 곳**을 고친다.

**(1)** import 를 더한다. 파일 맨 위를 아래로 바꾼다.

```tsx
import { useRef } from "react";
import type { PointerEvent as ReactPointerEvent } from "react";
import { IS_MAC } from "../bridge.ts";
import { WINDOW_CONTROLS_WIDTH } from "./WindowControls.tsx";
import { useWorkspace } from "../store/workspace.ts";
import { useDrag } from "../features/split/drag.ts";

/** 이만큼 안 움직이면 드래그가 아니라 클릭이다. GraphView 와 같은 값이다. */
const CLICK_SLOP = 3;
```

**(2)** `closeTab` 을 읽는 줄 **아래**에 두 줄을 더한다.

```tsx
const closeTab = useWorkspace((s) => s.closeTab);
const moveTabToPane = useWorkspace((s) => s.moveTabToPane);
/** 눌린 탭. moved 가 false 인 동안에는 아직 클릭일 수 있다. */
const pressRef = useRef<{ id: string; x: number; y: number; moved: boolean } | null>(null);
```

**(3)** `if (p === undefined …) return null;` **바로 위**에 핸들러 넷을 둔다.

```tsx
const onDown = (e: ReactPointerEvent<HTMLButtonElement>, id: string) => {
  e.currentTarget.setPointerCapture(e.pointerId);
  pressRef.current = { id, x: e.clientX, y: e.clientY, moved: false };
};

const onMove = (e: ReactPointerEvent<HTMLButtonElement>, title: string) => {
  const d = pressRef.current;
  if (d === null) return;

  if (!d.moved) {
    if (Math.abs(e.clientX - d.x) < CLICK_SLOP && Math.abs(e.clientY - d.y) < CLICK_SLOP) return;
    // 본문 영역은 드래그 시작 때 한 번만 잰다(설계 §5). main 은 두 칸을 함께 덮는다.
    const main = e.currentTarget.closest("main");
    if (main === null) return;
    const r = main.getBoundingClientRect();
    d.moved = true;
    useDrag.getState().start({ id: d.id, title }, { left: r.left, width: r.width });
  }

  useDrag.getState().move(e.clientX, e.clientY, useWorkspace.getState().panes.length);
};

const onUp = (e: ReactPointerEvent<HTMLButtonElement>) => {
  // 상태를 먼저 되돌린다: releasePointerCapture 는 더 이상 활성 포인터가 아닐 때
  // NotFoundError 를 던질 수 있는데, 뒤에 두면 핸들러가 무장 상태로 남는다(ResizeHandle 과 같은 이유).
  const d = pressRef.current;
  pressRef.current = null;
  if (e.currentTarget.hasPointerCapture(e.pointerId)) {
    e.currentTarget.releasePointerCapture(e.pointerId);
  }
  if (d === null) return;

  if (!d.moved) {
    focusTab(d.id);
    return;
  }

  const { zone, end } = useDrag.getState();
  end();
  if (zone !== null) moveTabToPane(d.id, zone);
};

const onCancel = () => {
  // 창 밖으로 나가거나 제스처가 취소된 것이다. 아무것도 옮기지 않는다.
  pressRef.current = null;
  useDrag.getState().end();
};
```

**(4)** 제목 버튼에서 `onClick` 을 떼고 포인터 핸들러를 단다. 닫기 버튼은 **건드리지 않는다.**

```tsx
<button
  type="button"
  onPointerDown={(e) => onDown(e, t.id)}
  onPointerMove={(e) => onMove(e, t.title)}
  onPointerUp={onUp}
  onPointerCancel={onCancel}
  aria-current={active ? "true" : undefined}
  // 드래그 중에 제목이 글자 선택으로 파래지는 것을 막는다.
  className="min-w-0 flex-1 select-none truncate px-2 text-left text-sm"
>
  {t.title}
</button>
```

- [ ] **Step 4: `Shell` 이 `DragLayer` 를 마운트한다**

`src/renderer/app/Shell.tsx` 에서 **두 곳**을 고친다.

**(1)** import 를 더한다. `import { Sidebar }` 줄 **아래**에 붙인다.

```tsx
import { DragLayer } from "../features/split/DragLayer.tsx";
```

**(2)** `</main>` **바로 아래**, 바깥 `</div>` **앞**에 한 줄을 넣는다.

```tsx
      </main>
      <DragLayer />
    </div>
```

- [ ] **Step 5: 정적 검사 넷을 돌린다**

```bash
npx prettier --write . && npm run lint && npm run typecheck && npm test
```

Expected: `lint`·`typecheck` 출력 없음. `test` 는 **`Tests 164 passed (164)`** — Task 4 와 같다.

- [ ] **Step 6: 앱을 띄워 사람이 확인한다**

Global Constraints 의 절차를 따른다. 띄운 뒤 **아래 열을 눈으로 본다.**

1. 탭을 오른쪽 끝으로 끌면 **드롭 존이 파랗게 강조되고**, 놓으면 칸이 갈라지나
2. 오른쪽 25%까지 안 갔을 때 놓으면 **아무 일도 안 일어나나**
3. 칸이 둘일 때 탭을 반대 칸으로 끌어 옮길 수 있나
4. 오른쪽 칸의 마지막 탭을 닫으면 칸이 도로 하나가 되나
5. **3px 안 움직이고 놓으면 클릭으로 처리되나** (탭이 그냥 활성화되나)
6. 드래그 중 창 밖으로 나갔다 놓으면 **아무 일도 안 일어나나**
7. **탭 줄의 빈 공간을 끌면 창이 여전히 끌리나** (설계 §3 — 여기가 가장 깨지기 쉽다)
8. 닫기 버튼이 드래그에 먹히지 않고 그대로 닫나
9. 왼쪽에 노트, 오른쪽에 쿼리 탭을 두고 둘 다 멀쩡히 보이나
10. 오른쪽 칸을 마지막으로 만진 뒤 사이드바에서 노트를 열면 **오른쪽에** 뜨나

그리고 그래프에 대해 **둘을 따로** 본다.

11. 그래프를 왼쪽에 두고 칸을 나눈 뒤, 왼쪽에서 다른 탭을 오갔다 돌아오면 **pan/zoom 이 유지되나**
12. 그래프 탭을 오른쪽 칸으로 끌면 **배치가 새로 잡히나** (설계 §7 — 의도된 동작이다)

**하나라도 어긋나면 멈추고 보고한다.** 고쳐서 넘기지 않는다.
비포·애프터 스크린샷은 **사용자에게 요청한다** (CLAUDE.md §7).

- [ ] **Step 7: 커밋한다**

```bash
git add src/renderer/features/split/drag.ts src/renderer/features/split/DragLayer.tsx \
        src/renderer/app/TabStrip.tsx src/renderer/app/Shell.tsx
git commit -F - <<'EOF'
feat: 탭을 끌어 칸을 나눈다

포인터 이벤트로 짓는다 — 탭 줄이 OS 드래그 띠 위라 HTML5 DnD 는
같은 픽셀을 다툰다. 드래그 상태는 전용 스토어에 둬서 포인터가 움직일
때마다 화면 전체가 다시 그려지지 않게 한다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

## 끝난 뒤

설계 §11 이 적은 것은 그대로 남는다 — 칸 너비 조절 · N칸/중첩/세로 분할 ·
같은 칸 안 재정렬 · 같은 노트를 양쪽 칸에 · 새 창으로 떼내기 · 분할 단축키 · 그래프 배치 이관.

다음 조각의 후보는 둘이다.

1. **LLM 연결** — 쿼리 탭 설계 §9 가 적은 것. `shared/ipc.ts` 채널 추가(FROZEN 개봉) · `main/keys.ts` · `core/llm/*` · 공급자 결정(ADR-0002 미결)
2. **노트 렌더링** — `docs/superpowers/specs/2026-09-16-note-rendering-design.md` 가 이미 있다 (아직 untracked)
