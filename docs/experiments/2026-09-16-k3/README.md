# K3 실측 — 돈이 나가는 실험을 제품 결정에 묶는다 (2026-09-16)

flash-lite(무료)로 30회차를 돌려 벽·검문·프롬프트를 다듬었고([어제 기록](../2026-09-15-밤샘실험/README.md)), 남은 천장은 **회차 편차**였다. 운영 모델은 팀 결정으로 Kimi K3. 이제부터는 실제 돈이므로 단계마다 "어느 제품 결정에 답하는가 · 통과 조건 · 멈춤 규칙 · 비용" 을 먼저 정하고 돌린다. 계획 원문은 `~/.claude/plans/snug-doodling-kurzweil.md`(로컬), 요지는 아래.

## 답하려는 결정 다섯

| #   | 결정                                                          | 단계      |
| --- | ------------------------------------------------------------- | --------- |
| D1  | K3 가 이 파이프라인에서 규모로 되는가 (스키마·검문·실패 유형) | S1b       |
| D2  | 편차가 모델 탓이었나 — 호출 안 샘플링 vs 경로 의존            | S1a · S1b |
| D3  | 비용 모델: 노트 1장 · 논문 1편 · 초기 인입 1000장             | S1b · S5  |
| D4  | 제품 기본값: 모델(K3/k2.6) · reasoning_effort · 프롬프트 판   | S2~S4     |
| D5  | 실제 노트에서도 쓸 만한가 (사람 평가)                         | S7        |

## 판정 규칙 — 퍼센트가 아니라 항목으로

연구자 볼트 정답이 69항목(보강 전 36)이라 링크 하나가 몇 점씩 움직인다. flash-lite 같은 설정 쌍에서 이미 3~9항목이 뒤집혔다. 그래서 같은 설정 두 회차의 **안정 핵**(둘 다 맞힘) · **뒤집힘**(한쪽만) · **못 맞힘**(둘 다 못) 을 `compare.ts` 가 세고:

- **비열등** (등가면 충분: k2.6 · 프롬프트 축소) — 안정 핵에서 ≤1 빠지고 금지 페이지 100%
- **우월** (돈이 더 드는 쪽: effort) — 못 맞힘에서 ≥2 새로 맞힘. 뒤집힌 항목을 맞힌 것은 동전
- **열등** — 안정 핵에서 ≥3 빠짐
- **"K3 가 편차를 줄였다"** = 같은 설정 두 회차의 뒤집힘 ≤2

flash-lite 기준(보강된 정답으로 $0 재계산): 연구자 28·30회차 안정 핵 48 · 뒤집힘 9 · 못 맞힘 12 (69). 학부생 26·27회차 안정 핵 63 · 뒤집힘 11 · 못 맞힘 4 (78).

## 돌리기 전에 고친 것 (S0.5, 커밋 c7dbae9)

- **검문 결함**: `normQuote` 가 마크다운 강조를 안 벗겨 노트의 `23.5% → **14.0%**` 와 quote `23.5% → 14.0%` 가 안 맞았다. 정답 사실이 세 회차에서 `quote-없음` 으로 버려졌다. 이걸 두고 "K3 지적 0 vs flash-lite 58" 을 모델 차이로 읽으면 안 됐다
- **캐시 키에 샘플링**: effort 가 키에 없어 high 회차가 low 의 캐시를 재생해 $0 에 "차이 없음" 이 나올 수 있었다
- **`--no-cache` 가 재개까지 껐다**: 처리한 장을 다시 불렀다 — 첫 실측에서 한 장 $0.03 을 두 번 냈다. 응답 캐시와 재개를 분리하고 재정리는 `--force`
- 예산 상한 `--budget`(넘으면 다음 호출 전에 멈춤) · `--expect`(항목 수가 다르면 호출 전 중단) · `--limit`(앞 N장) · Kimi 타임아웃 600s 재시도 1회 · `max_tokens` 32k · 항목별 스키마 실패 처리 · 호출당 경과 시간 · 로그 머리에 프롬프트·정답 지문
- 연구자 볼트 정답 세트 36 → 69항목 (노트에서만, 출처는 `fixtures/README.md`)

## 회차

| 회차 | 단계 | 설정                     | 호출 | 비용  | 페이지 | 금지 | 링크      | 사실      | 지적 | 중앙값 시간 |
| ---- | ---- | ------------------------ | ---- | ----- | ------ | ---- | --------- | --------- | ---- | ----------- |
| 31   | S0   | K3 low · 학부생 1장      | 1    | $0.07 | —      | —    | —         | —         | 0    | —           |
| 28   | 기준 | flash-lite · BM25·8      | 29   | $0    | 15/19  | 7/7  | 14/20     | 19/23     | 31   | 몇 초       |
| 30   | 기준 | flash-lite · BM25·5      | 25   | $0    | 15/19  | 7/7  | 12/20     | 16/23     | 33   | 몇 초       |
| 32   | S1b  | **K3 low** · 연구자 30장 | 32   | $2.62 | 18/19  | 7/7  | **20/20** | **22/23** | 17   | 64s         |

(28·30 은 보강된 정답 세트로 $0 재계산. 32 의 호출 32 = 노트 30 + `나` 요약 1 + 재개 실수 1.)

### 32회차 — K3 low 첫 회차: 67/69

`compare.ts --base 28 30 --test 32`: flash-lite 안정 핵 48 중 **1개만 놓치고**(`한글 수사 정규화` 페이지 — K3 는 exp-007 절에 넣었다), flash-lite 가 두 번 다 못 맞힌 12개 중 **11개를 맞혔다.** 판정 "비열등 · 우월". 새로 맞힌 것은 전부 노트 두 장 이상을 이어야 나오는 것들이다 — `exp-006 ↔ exp-007`, `exp-005 ↔ SummaC`, `AIHub ↔ exp-001`, `박사 진학` 페이지, 논문 제목 "Numbers Lie First". flash-lite 의 천장이 "노트를 가로지르는 연결" 이었는데 K3 는 그것을 한다.

정답 세트 손질 둘 (전 회차 소급, 호출 0):

- `must_not_pages` 를 정확 일치로. 앞부분 일치 때문에 `논문`(그릇) 이 정당한 `논문 초안` 을 잡았다 — 잣대의 버그
- `소연` 에 대안 `소연이`. K3 가 사람 페이지를 조사 붙은 이름으로 만들었다. 같은 사람이므로 이름만 다른 실패이고 계획의 규칙대로 대안에 넣었다. 다만 **이름에 조사가 붙은 것은 보이는 오류**로 남는다 — 규칙 2("이름만") 가 있는데도. 2회차에서 또 나오면 센다

**D1 — K3 는 된다.** 스키마 실패 0, 절-보호·나-기록-중복·링크-해제 지적 0 (flash-lite 는 회차당 10~20건). 남은 지적 16건은 전부 `quote-없음`:

| 원인                                | 건수 | 예                                                                         |
| ----------------------------------- | ---- | -------------------------------------------------------------------------- |
| **베끼다 낸 오타** (한 음절이 바뀜) | 9    | 아무도→아묵도 · 내년→낸년·난년 · 자르면→자륵면 · 무난→물난 · 개체명→개척명 |
| 빈 quote                            | 4    | `""`                                                                       |
| `...` 로 이어 붙인 두 토막          | 1    |                                                                            |
| 기타                                | 2    |                                                                            |

오타 9건은 검문이 정상 차단했지만 사실 자체는 맞았을 것이라 **기록 9줄을 잃었다.** flash-lite 에는 없던 실패 유형이다 — 추론 모델이 한국어를 글자 그대로 베끼다 음절을 바꾼다. 벽을 지금 풀지는 않는다(비교가 흐려진다). S4a(effort high)에서 줄어드는지 보고, 안 줄면 "치환 1음절/15자 이내는 통과" 같은 좁은 허용을 검토한다. 빈 quote 4건은 스키마에 `minLength: 1` 을 두면 모델이 빈 값을 못 낸다 — 다음 판.

**D3 — 노트당 비용.** 30장 $2.55(재개 실수 빼고), 노트당 평균 $0.082 (첫 장 $0.05 → 후반 $0.11, 위키가 커져 후보 8장이 무거워진다). 호출 중앙값 64초, 30장에 API 시간 34분. 프리픽스 캐시는 32회 중 4회만 잡혔다(`캐시 6912`) — 이유 미상, 실제 청구로 확인.

**품질 표본.** `exp-007` 페이지가 "첫 시도 결과 / 속도 문제" 절로 나뉘고 exp-007b 의 수치가 그 안에 들어갔다. `나` 요약이 20장째 갱신에서 "exp-007 검증 디코딩으로 14.0%까지" 로 현재를 말한다. 허브 뺀 밀도 5.47 (flash-lite 2.6) — 절반은 `autoLink` 가 같은 이름을 문단마다 다시 걸어서다.

## 관찰

- **K3 는 느리다.** effort low 에서 호출당 37~57초 (flash-lite 는 몇 초). 30장이면 25분, 86장이면 한 시간. 제품에서 인입은 백그라운드여야 하고 진행률·재개가 필요하다 — `sync_state` 가 재개를 이미 준다
- **프리픽스 캐시는 가끔만 잡힌다.** 같은 시스템 프롬프트(6,912 토큰)를 매번 보내는데 `cached_tokens` 가 32회 중 4회만 찍혔다. 잡히면 그 몫이 $0.3/1M 로 내려가 호출당 $0.02 (입력의 40%) 가 준다. 어림은 usage 가 말하는 대로 계산하고(안 잡힌 호출은 미스 단가), 실제 청구는 콘솔로 대조한다
- **잔액 반영이 늦다.** 호출 뒤 십여 분이 지나도 `/users/me/balance` 가 그대로다. 실행 끝의 잔액 줄은 참고만, 진짜 값은 다음 실행의 시작 잔액과 콘솔
